---
description: 论文数字化平台仓库地图与开发约定，Agent 默认上下文
alwaysApply: true
---

# 项目上下文

**本仓库**：论文服务业务轻量工作台 `thesis-digital-platform`，业务说明见根目录 `README.md`，Agent 协作规范见 `AGENTS.md`。

## 技术栈

| 层 | 技术 |
|----|------|
| 框架 | Next.js 15 App Router + React 19 |
| 语言 | TypeScript（strict） |
| 样式 | Tailwind CSS 3 + 自定义 glass UI |
| 校验 | Zod（`src/lib/validation.ts`） |
| 存储 | 本地 JSON 文件（`data/*.json`，gitignore） |
| 导出 | xlsx |

## 目录速查

| 路径 | 用途 |
|------|------|
| `src/app/(dashboard)/` | 总览、客户、工单、写手页面 |
| `src/app/api/` | REST Route Handlers（CRUD + Excel 导出） |
| `src/components/` | 按业务域划分的 UI 组件 |
| `src/lib/types.ts` | 领域模型（Client / Order / Writer） |
| `src/lib/repositories/` | Repository 接口 + JSON 实现 |
| `src/lib/server/` | 服务端工具（storage、Excel、order-drafts） |
| `src/lib/analytics.ts` | 总览页纯函数聚合 |
| `scripts/` | 历史 Excel 导入/修复脚本 |
| `data/` | 运行时 JSON 数据（首次启动从 mock-data 种子初始化） |

## 业务铁律

1. **先建客户，再开工单** — 工单只能从客户上下文创建（`POST /api/clients/[id]/create-order`），`POST /api/orders` 返回 405。
2. **写手独立维护** — 写手池与客户档案解耦，派单时在工单里关联。
3. **Repository 是唯一数据入口** — 页面（RSC）与 API 均通过 `src/lib/repositories/index.ts` 访问数据，禁止绕过 repository 直接读写 JSON。

## 调查代码的默认顺序

1. **本仓库**：优先用 **codegraph** MCP（`codegraph_context` → 按需 `codegraph_trace` / `codegraph_explore`）。
2. **业务规则**：`README.md` + `.cursor/skills/thesis-platform-dev/knowledge/`。
3. **API 契约**：`src/lib/validation.ts` + 对应 `src/app/api/**/route.ts`。

## 回答风格

用中文；先基于代码事实再给实现步骤；引用文件用可点击路径。
